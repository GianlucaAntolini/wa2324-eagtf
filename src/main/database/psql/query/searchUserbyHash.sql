SELECT *
FROM "User"
WHERE email = 'user@example.com' -- Replace 'user@example.com' with the provided username/email
  AND password = 'hashed_password'; -- Replace 'hashed_password' with the hashed version of the provided password
